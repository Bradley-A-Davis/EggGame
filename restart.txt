docker stop egg-dev

docker run --rm -it --name egg-dev \
  -p 5176:5176 \
  -v "$HOME/remote_mount/Private/Server1/Documents/EggGame/app":/app \
  -v vite_node_modules:/app/node_modules \
  -w /app \
  node:20-alpine sh -lc \
  "npm run dev -- --host 0.0.0.0 --port 5176"
